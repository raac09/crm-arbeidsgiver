const fs = require("fs");
const activityKoder = require("./AKTIVITETKODER");
const TemaMapping = require("./TemaMapping");
const TypeMapping = require("./TypeMapping");


activityKoder.forEach(kode=>{
  tabell.push()
})
console.log(activityKoder);